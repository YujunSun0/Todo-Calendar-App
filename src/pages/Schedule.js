import Calendars from "../component/Calendars";

const Schedule = ({ selectedData, listData }) => {
  return <Calendars selectedData={selectedData} listData={listData} />;
};

export default Schedule;
